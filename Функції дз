#include <iostream>
using namespace std;

/*bool IsLeapYear(int year)
{
    if (year % 400 == 0)
    {
        return true;
    }
    if (year % 100 == 0)
    {
        return false;
    }
    if (year % 4 == 0)
    {
        return true;
    }

    return false;
}

int DaysInMonth(int month, int year)
{
    if (month == 2)
    {
        if (IsLeapYear(year))
        {
            return 29;
        }
        else
        {
            return 28;
        }
    }

    if (month == 4 || month == 6 || month == 9 || month == 11)
    {
        return 30;
    }

    return 31;
}

int DateToDays(int day, int month, int year)
{
    int days = 0;

    for (int i = 1; i < year; i++)
    {
        if (IsLeapYear(i))
        {
            days += 366;
        }
        else
        {
            days += 365;
        }
    }

    for (int i = 1; i < month; i++)
    {
        days += DaysInMonth(i, year);
    }

    days += day;

    return days;
}

int DifferenceDays(int day1, int month1, int year1,
                   int day2, int month2, int year2)
{
    int date1 = DateToDays(day1, month1, year1);
    int date2 = DateToDays(day2, month2, year2);

    if (date1 > date2)
    {
        return date1 - date2;
    }
    else
    {
        return date2 - date1;
    }
}

int main()
{
    int day1 = 10;
    int month1 = 9;
    int year1 = 2026;

    int day2 = 20;
    int month2 = 9;
    int year2 = 2026;

    cout << "Difference = "
         << DifferenceDays(day1, month1, year1,
                           day2, month2, year2)
         << " days" << endl;

    return 0;
}*/


/*double AverageArray(int arr[], int size)
{
    int sum = 0;

    for (int i = 0; i < size; i++)
    {
        sum += arr[i];
    }

    return (double)sum / size;
}

int main()
{
    const int size = 10;
    int arr[size] = { 5, 8, 12, 3, 7, 10, 4, 6, 9, 11 };

    cout << "Average elements = "
         << AverageArray(arr, size)
         << endl;

    return 0;
}*/


void CountElements(int arr[], int size)
{
    int pos = 0;
    int neg = 0;
    int zero = 0;

    for (int i = 0; i < size; i++)
    {
        if (arr[i] > 0)
        {
            pos++;
        }
        else if (arr[i] < 0)
        {
            neg++;
        }
        else
        {
            zero++;
        }
    }

    cout << "Positive elements = " << pos << endl;
    cout << "Negative elements = " << neg << endl;
    cout << "Zero elements = " << zero << endl;
}

int main()
{
    const int size = 10;
    int arr[size] = { 5, -3, 0, 8, -7, 0, 4, -2, 9, 0 };

    CountElements(arr, size);

    return 0;
}
